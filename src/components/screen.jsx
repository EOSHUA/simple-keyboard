import React from 'react';

    function Screen({currentText}) {
        return (
            <div className="screen">
                <h1>{currentText}</h1>
            </div>
        );
    }
    
    
    export default Screen;